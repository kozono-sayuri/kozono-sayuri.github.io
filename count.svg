<svg xmlns="http://www.w3.org/2000/svg" width="170" height="32" viewBox="0 0 170 32">
  <defs>
    <!-- 粉色漸層背景，跟網站按鈕風格接近 -->
    <linearGradient id="badge-bg" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#ffb4e4" />
      <stop offset="100%" stop-color="#ff78c2" />
    </linearGradient>
    <!-- 一點柔和的陰影 -->
    <filter id="soft-shadow" x="-20%" y="-50%" width="140%" height="200%">
      <feDropShadow dx="0" dy="2" stdDeviation="2" flood-color="rgba(0,0,0,0.18)" />
    </filter>
  </defs>

  <!-- 圓角底牌 -->
  <g filter="url(#soft-shadow)">
    <rect x="0.5" y="0.5" width="169" height="31" rx="16"
          fill="url(#badge-bg)"
          stroke="rgba(255,255,255,0.8)" stroke-width="1" />
  </g>

  <!-- 左側小圖示 + 文字 Visitors -->
  <text x="20" y="20"
        fill="#ffffff"
        font-family="-apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
        font-size="12"
        font-weight="600">
    👀 Visitors
  </text>

  <!-- 右側嵌入 Glitch 的動態計數 PNG（這個會自動更新數字） -->
  <image
    href="https://profile-counter.glitch.me/kozono-sayuri/count.png"
    x="96" y="5"
    height="22"
    preserveAspectRatio="xMidYMid meet" />
</svg>
